kind = "mobile"
previewPath = "/mobile/"
title = "EduPlatform Mobile — Học viên"
version = "1.0.0"
id = "artifacts/mobile-app"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 24382

[services.development]
run = "pnpm --filter @workspace/mobile-app run dev"

[services.env]
PORT = "24382"
BASE_PATH = "/mobile/"
